# orison-location-presentation

Projects scene, camera, audio and effects; caches immutable scene output by room.

Public factory: `createLocationPresentationKit`. Install through src/composition/create-modular-game.js. Required APIs are verified there and by the production gates. Never add a parallel game state or timer. Update tests/integration when changing this boundary.
