# orison-presentation-projection

Projects menus, inventory, journal, panels and gameplay controls through Nexus UI.

Public factory: `createProjectionKit`. Install through src/composition/create-modular-game.js. Required APIs are verified there and by the production gates. Never add a parallel game state or timer. Update tests/integration when changing this boundary.
