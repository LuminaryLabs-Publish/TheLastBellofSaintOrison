# Reassemble the Class Photograph

**SH-01 · puzzle · required · specified, not implemented**

Location: School Hall. Runtime source: [pinned location content](https://github.com/LuminaryLabs-Publish/TheLastBellofSaintOrison/tree/aaaec2d1ccb5856ccef9395f090653a7e2efca1f/src/kits/locations/school-hall/content). Canonical proposal: [encounters.json](../../encounters.json). Generated by `node tools/design/build-immersive.mjs`.

## Player purpose

Establish four identities from paired records.

## Entry and first impression

Arrive from bell-tower-base. This encounter can be examined immediately; commits follow its stated prerequisites.

Four photograph fragments sit beside matching admission seals; chosen-alias strips remain attached.

## Objects and controls

- photo-pieces: place or select pairs
- seal-view: enlarge matching marks
- alias-tab: keep attached

## Obtainable clues

- Each fragment has a unique torn edge and matching record seal.
- The resulting pairs identify June Arlen, Tomas Reed, Ada Wren and Niko Bell; names alone are not the matching key.

## Exact action sequence

1. Match fragments to their torn edges and record seals.
2. Inspect each photograph beside its independent admission record.
3. Retain the attached alias strip on every pair.

## Wrong attempts and assistance

A false pair leaves the torn seam and seal misaligned; never erase an alias to make it fit.

Hints progress only on request:

1. Match physical evidence first.
2. Use the admission seal as a second check.
3. Complete all four photo-and-record pairs with aliases intact.

## Completion

names-first-four recorded from four corroborated pairs; no additional children invented.

## Supported alternatives

The exact sequence above is the supported logical solution. Pointer, keyboard and assisted controls are equivalent input paths, not alternate story outcomes. Different persistent outcomes exist only where the listed decision explicitly offers them; no extra solution is implied.

## Existing story and state mapping

Does not independently mark the legacy room primary objective complete. Keep source clue IDs names-first-four. Observe mapped source objects only after the corresponding fact is actually presented; never auto-award all clues on room entry.

```json
{
  "requiredClues": [
    "names-first-four"
  ],
  "sourceObjects": [
    "photos"
  ],
  "primaryCompletion": false,
  "effects": [],
  "departureChoices": [],
  "note": "Does not independently mark the legacy room primary objective complete. Keep source clue IDs names-first-four. Observe mapped source objects only after the corresponding fact is actually presented; never auto-award all clues on room entry."
}
```

## Pressure and accessible operation

Use the shared room exposure budget, initially the source allocation of 165 seconds, not a fresh full timer per encounter. Advance only during active world manipulation after a safe clue demonstration. At 50% show the room warning with a stable warning icon: “Chalk taps the same number against every desk.” At 80% show its critical caption and distinct double-border marker: “The slate begins to write your childhood name.” At the room limit release held controls safely and use the established retreat: You turn the slate face down and stand beside the open corridor door. Never destroy required evidence. Menus, hints, required captions/consequence explanations and user-requested journal reading pause exposure; normal close inspection keeps the world active unless a required explanation is on screen. Source allocations are tuning starting points, not measured encounter durations. Passive warning icons and ambient captions do not pause time; only an explicitly opened required explanation does.

Controls photo-pieces, seal-view, alias-tab must each have a focusable name and a visible response. Pointer drag/rotation has discrete click controls and keyboard previous/next/confirm equivalents. Holds offer press-to-latch and press-to-release assistance; release on blur, pause or scene exit cannot commit a destructive action. Caption all significant sounds; preserve symbol or shape distinctions independently of color. Replay demonstrations by user-stepped stages when timing is assisted. Keep clue text at supported large scale with paging inside the inspection area, and retain a stable back/exit action. Reduced motion replaces continuous movement with labelled state changes while preserving clue order.

## Save, reset and cancellation

Proposed Investigation-owned logical record: encounterId=SH-01, definitionVersion=1, confirmedStep (0 through 3), completed, and a validated control-value record keyed only by photo-pieces, seal-view, alias-tab. Decision records additionally store a preview selection separately from the single Narrative/Campaign commit. This is a proposed schema extension, not an existing installed API. Snapshot logical state plus owning-domain effects at one safe commit boundary. Never save DOM nodes, callbacks, drag pointers, audio handles, frame timestamps or an animation position as authority. On restore, reconstruct controls from confirmed state and replay only noncommitting feedback; never award a reward again. An interrupted unconfirmed drag/hold returns to its last stable detent. A wrong attempt resets only the local unsolved control sequence specified above; verified clues, items, primary completion and committed choices remain. Validate the whole snapshot and definition version before replacing any live owner; an unknown version leaves the current session unchanged. Entry/exit removes input subscriptions, stops local audio and cancels uncommitted operation receipts. Retry preserves original branch commitments and can always return to the named shelter.

## Presentation and asset requests

- `orison.design.sh-01.photo-pieces` — photo-pieces: place or select pairs. Place in the encounter’s named interaction zone with the clue reference visible from the same close view. Use the operation and obstruction described in action steps; do not substitute a decorative prop. Variants: idle, focused, blocked-with-reason, stable-after-action. Fallback: Labelled procedural proxy with the same shape/symbol and focus control; never remove its gameplay input.
- `orison.design.sh-01.seal-view` — seal-view: enlarge matching marks. Place in the encounter’s named interaction zone with the clue reference visible from the same close view. Use the operation and obstruction described in action steps; do not substitute a decorative prop. Variants: idle, focused, blocked-with-reason, stable-after-action. Fallback: Labelled procedural proxy with the same shape/symbol and focus control; never remove its gameplay input.
- `orison.design.sh-01.alias-tab` — alias-tab: keep attached. Place in the encounter’s named interaction zone with the clue reference visible from the same close view. Use the operation and obstruction described in action steps; do not substitute a decorative prop. Variants: idle, focused, blocked-with-reason, stable-after-action. Fallback: Labelled procedural proxy with the same shape/symbol and focus control; never remove its gameplay input.
- `orison.design.sh-01.clue-panel` — Required clue close-up: Each fragment has a unique torn edge and matching record seal. The resulting pairs identify June Arlen, Tomas Reed, Ada Wren and Niko Bell; names alone are not the matching key. Variants: unexamined, exposed, recorded. Fallback: Readable diagram and faithful caption containing the same evidence.
- `orison.design.sh-01.feedback` — Cue sheet: an invalid action gives the specified wrong-attempt response; each accepted step gives its stated physical result. Final result: names-first-four recorded from four corroborated pairs; no additional children invented. Variants: accepted-step, blocked-step, completed. Fallback: Caption and stable state marker for every audible cue.

## Implementation task pack

Task IM-SH-01; owner unassigned; status specified, implementation queued. Location boundary: src/kits/locations/school-hall/content, assets and sequences. Investigation owns mechanical progress and discovered evidence; Inventory owns items; Narrative owns choices/relationships; Campaign and Scene keep their existing completion/travel boundaries. Presentation owns control and cue descriptors. Dependencies: CAP-INPUT, CAP-MECHANISM, CAP-PROJECTION, CAP-SAVE and CAP-SEQUENCE. Physical controls and new logical state are proposed capabilities; current orisonOperations supports only observe/combine/use/dialogue/choose/solve/complete. Do not call new operation names or bypass source prerequisite checks as if they were installed.

1. Read SH-01 and its source mapping. Load the actual school-hall incoming fixture; record current required clues and departure guard.
2. Create the declared control definitions for photo-pieces, seal-view, alias-tab only after the shared input and state capabilities pass their tasks.
3. Bind each exact action step to a validated Investigation transition. Place required clues in the authored scene before their observation receipts are possible.
4. Project the accepted and blocked state changes through Presentation; use the listed procedural fallbacks until approved source assets exist.
5. Coordinate finite completion via verified receipt waits; keep pressure inside Threat and do not put frame callbacks in a location Sequence.
6. Commit existing source effects at the specified stage; preserve idempotency across reward, primary completion and departure.
7. Implement the named restore and cancellation cases, then run the encounter and route acceptance walkthroughs from legal input.
8. Attach source revision, observed behavior, screenshots and outstanding assets. Mark implemented only after these runtime gates pass.

## Acceptance walkthrough

1. Enter school-hall with its source-valid incoming state and the stated prerequisites. Before acting, confirm that none of this encounter’s unearned effects are already granted.
2. Locate the named controls from the wide view, then expose these clues: Each fragment has a unique torn edge and matching record seal. The resulting pairs identify June Arlen, Tomas Reed, Ada Wren and Niko Bell; names alone are not the matching key.
3. Use the wrong or cancelled action described here: A false pair leaves the torn seam and seal misaligned; never erase an alias to make it fit. Confirm required items, evidence and prior choices remain unchanged.
4. Perform: Match fragments to their torn edges and record seals. Verify this response before continuing; a text-only success flag is insufficient for runtime acceptance.
5. Perform: Inspect each photograph beside its independent admission record. Verify this response before continuing; a text-only success flag is insufficient for runtime acceptance.
6. Perform: Retain the attached alias strip on every pair. Verify this response before continuing; a text-only success flag is insufficient for runtime acceptance.
7. Check the owner result: names-first-four recorded from four corroborated pairs; no additional children invented. Discovery mapping: names-first-four. Compare against the exact mapping section; do not grant an entire room’s clues automatically.
8. Repeat the final accepted input twice, then save and restore at every stable step from 0 through 3. Confirm one reward/choice only, and reconstruct the same logical control state.
9. Cancel during the first uncommitted manipulation and lose window focus during a hold. Return to the last confirmed state; the documented shelter remains available.
10. Repeat with audio muted, shape labels instead of color, keyboard-only controls, largest supported text, assisted timing and Canvas fallback. Each required clue and action must still be available.
11. Skip all optional lore and prove the encounter is still solvable using its visible clues and caption alternatives.

## Related contracts

[Location flow](encounter-flow.md) · [Asset manifest](presentation-and-assets.md) · [Location build tasks](implementation-task-pack.md) · [Review](acceptance-and-review.md) · [Shared puzzle contract](../../gameplay-systems/physical-puzzle-contract.md).

Design review acceptance does not mean runtime, visual, audio, timing or player-comprehension acceptance.
