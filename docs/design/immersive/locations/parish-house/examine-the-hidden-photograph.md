# Examine the Hidden Photograph

**PH-03 · investigation · optional · specified, not implemented**

Location: Parish House. Runtime source: [pinned location content](https://github.com/LuminaryLabs-Publish/TheLastBellofSaintOrison/tree/aaaec2d1ccb5856ccef9395f090653a7e2efca1f/src/kits/locations/parish-house/content). Canonical proposal: [encounters.json](../../encounters.json). Generated by `node tools/design/build-immersive.mjs`.

## Player purpose

Give the family discovery a personal response.

## Entry and first impression

Arrive from chapel-nave. Complete PH-01 first. Also complete the second required encounter before departure; this optional encounter never supplies its prerequisites.

A photograph behind the folded letter shows Mara and Elian beside the same old papers.

## Objects and controls

- photo-flap: lift
- conversation: choose admission or silence
- journal: retain optional detail

## Obtainable clues

- The photo supports the already-established family connection, not a new plot reversal.
- Elian responds in person if released, otherwise through the line.

## Exact action sequence

1. Lift the photo flap and compare it with the folded letter.
2. Optionally admit what Mara burned in a brief exchange.
3. Let Elian respond without automatic forgiveness; preserve the letter departure option.

## Wrong attempts and assistance

Closing the flap skips the conversation without removing required family knowledge.

Hints progress only on request:

1. The required fact is already known.
2. This is a chance to respond, not a new lock.
3. Choose the admission if you want the conversation.

## Completion

Optional elianTrust may be committed via the existing letter choice; silence remains a valid continuation.

## Supported alternatives

The exact sequence above is the supported logical solution. Pointer, keyboard and assisted controls are equivalent input paths, not alternate story outcomes. Skipping is supported and retains all mandatory progression.

## Existing story and state mapping

Does not independently mark the legacy room primary objective complete. Keep source clue IDs unchanged; no new required clue is awarded here. Observe mapped source objects only after the corresponding fact is actually presented; never auto-award all clues on room entry.

```json
{
  "requiredClues": [],
  "sourceObjects": [],
  "primaryCompletion": false,
  "effects": [],
  "departureChoices": [
    "utility",
    "letter"
  ],
  "note": "Does not independently mark the legacy room primary objective complete. Keep source clue IDs unchanged; no new required clue is awarded here. Observe mapped source objects only after the corresponding fact is actually presented; never auto-award all clues on room entry.",
  "legacyOptionalActions": []
}
```

## Pressure and accessible operation

Use the shared room exposure budget, initially the source allocation of 165 seconds, not a fresh full timer per encounter. Advance only during active world manipulation after a safe clue demonstration. At 50% show the room warning with a stable warning icon: “Footsteps stop outside the study whenever you stop reading.” At 80% show its critical caption and distinct double-border marker: “The handle turns once, although the stair is empty.” At the room limit release held controls safely and use the established retreat: You move into the utility vestibule and secure its mechanical bolt. Never destroy required evidence. An optional detour may cost active time, but skipping it changes no required gate. Menus, hints, required captions/consequence explanations and user-requested journal reading pause exposure; normal close inspection keeps the world active unless a required explanation is on screen. Source allocations are tuning starting points, not measured encounter durations. Passive warning icons and ambient captions do not pause time; only an explicitly opened required explanation does.

Controls photo-flap, conversation, journal must each have a focusable name and a visible response. Pointer drag/rotation has discrete click controls and keyboard previous/next/confirm equivalents. Holds offer press-to-latch and press-to-release assistance; release on blur, pause or scene exit cannot commit a destructive action. Caption all significant sounds; preserve symbol or shape distinctions independently of color. Replay demonstrations by user-stepped stages when timing is assisted. Keep clue text at supported large scale with paging inside the inspection area, and retain a stable back/exit action. Reduced motion replaces continuous movement with labelled state changes while preserving clue order.

## Save, reset and cancellation

Proposed Investigation-owned logical record: encounterId=PH-03, definitionVersion=1, confirmedStep (0 through 3), completed, and a validated control-value record keyed only by photo-flap, conversation, journal. Decision records additionally store a preview selection separately from the single Narrative/Campaign commit. This is a proposed schema extension, not an existing installed API. Snapshot logical state plus owning-domain effects at one safe commit boundary. Never save DOM nodes, callbacks, drag pointers, audio handles, frame timestamps or an animation position as authority. On restore, reconstruct controls from confirmed state and replay only noncommitting feedback; never award a reward again. An interrupted unconfirmed drag/hold returns to its last stable detent. A wrong attempt resets only the local unsolved control sequence specified above; verified clues, items, primary completion and committed choices remain. Validate the whole snapshot and definition version before replacing any live owner; an unknown version leaves the current session unchanged. Entry/exit removes input subscriptions, stops local audio and cancels uncommitted operation receipts. Retry preserves original branch commitments and can always return to the named shelter.

## Presentation and asset requests

- `orison.design.ph-03.photo-flap` — photo-flap: lift. Place in the encounter’s named interaction zone with the clue reference visible from the same close view. Use the operation and obstruction described in action steps; do not substitute a decorative prop. Variants: idle, focused, blocked-with-reason, stable-after-action. Fallback: Labelled procedural proxy with the same shape/symbol and focus control; never remove its gameplay input.
- `orison.design.ph-03.conversation` — conversation: choose admission or silence. Place in the encounter’s named interaction zone with the clue reference visible from the same close view. Use the operation and obstruction described in action steps; do not substitute a decorative prop. Variants: idle, focused, blocked-with-reason, stable-after-action. Fallback: Labelled procedural proxy with the same shape/symbol and focus control; never remove its gameplay input.
- `orison.design.ph-03.journal` — journal: retain optional detail. Place in the encounter’s named interaction zone with the clue reference visible from the same close view. Use the operation and obstruction described in action steps; do not substitute a decorative prop. Variants: idle, focused, blocked-with-reason, stable-after-action. Fallback: Labelled procedural proxy with the same shape/symbol and focus control; never remove its gameplay input.
- `orison.design.ph-03.clue-panel` — Required clue close-up: The photo supports the already-established family connection, not a new plot reversal. Elian responds in person if released, otherwise through the line. Variants: unexamined, exposed, recorded. Fallback: Readable diagram and faithful caption containing the same evidence.
- `orison.design.ph-03.feedback` — Cue sheet: an invalid action gives the specified wrong-attempt response; each accepted step gives its stated physical result. Final result: Optional elianTrust may be committed via the existing letter choice; silence remains a valid continuation. Variants: accepted-step, blocked-step, completed. Fallback: Caption and stable state marker for every audible cue.

## Implementation task pack

Task IM-PH-03; owner unassigned; status specified, implementation queued. Location boundary: src/kits/locations/parish-house/content, assets and sequences. Investigation owns mechanical progress and discovered evidence; Inventory owns items; Narrative owns choices/relationships; Campaign and Scene keep their existing completion/travel boundaries. Presentation owns control and cue descriptors. Dependencies: CAP-INPUT, CAP-MECHANISM, CAP-PROJECTION, CAP-SAVE and CAP-SEQUENCE. Physical controls and new logical state are proposed capabilities; current orisonOperations supports only observe/combine/use/dialogue/choose/solve/complete. Do not call new operation names or bypass source prerequisite checks as if they were installed.

1. Read PH-03 and its source mapping. Load the actual parish-house incoming fixture; record current required clues and departure guard.
2. Create the declared control definitions for photo-flap, conversation, journal only after the shared input and state capabilities pass their tasks.
3. Bind each exact action step to a validated Investigation transition. Place required clues in the authored scene before their observation receipts are possible.
4. Project the accepted and blocked state changes through Presentation; use the listed procedural fallbacks until approved source assets exist.
5. Coordinate finite completion via verified receipt waits; keep pressure inside Threat and do not put frame callbacks in a location Sequence.
6. Commit existing source effects at the specified stage; preserve idempotency across reward, primary completion and departure.
7. Implement the named restore and cancellation cases, then run the encounter and route acceptance walkthroughs from legal input.
8. Attach source revision, observed behavior, screenshots and outstanding assets. Mark implemented only after these runtime gates pass.

## Acceptance walkthrough

1. Enter parish-house with its source-valid incoming state and the stated prerequisites. Before acting, confirm that none of this encounter’s unearned effects are already granted.
2. Locate the named controls from the wide view, then expose these clues: The photo supports the already-established family connection, not a new plot reversal. Elian responds in person if released, otherwise through the line.
3. Use the wrong or cancelled action described here: Closing the flap skips the conversation without removing required family knowledge. Confirm required items, evidence and prior choices remain unchanged.
4. Perform: Lift the photo flap and compare it with the folded letter. Verify this response before continuing; a text-only success flag is insufficient for runtime acceptance.
5. Perform: Optionally admit what Mara burned in a brief exchange. Verify this response before continuing; a text-only success flag is insufficient for runtime acceptance.
6. Perform: Let Elian respond without automatic forgiveness; preserve the letter departure option. Verify this response before continuing; a text-only success flag is insufficient for runtime acceptance.
7. Check the owner result: Optional elianTrust may be committed via the existing letter choice; silence remains a valid continuation. Discovery mapping: no new required clue. Compare against the exact mapping section; do not grant an entire room’s clues automatically.
8. Repeat the final accepted input twice, then save and restore at every stable step from 0 through 3. Confirm one reward/choice only, and reconstruct the same logical control state.
9. Cancel during the first uncommitted manipulation and lose window focus during a hold. Return to the last confirmed state; the documented shelter remains available.
10. Repeat with audio muted, shape labels instead of color, keyboard-only controls, largest supported text, assisted timing and Canvas fallback. Each required clue and action must still be available.
11. Skip the encounter completely and prove the following required encounter and at least one ending remain reachable.

## Related contracts

[Location flow](encounter-flow.md) · [Asset manifest](presentation-and-assets.md) · [Location build tasks](implementation-task-pack.md) · [Review](acceptance-and-review.md) · [Shared puzzle contract](../../gameplay-systems/physical-puzzle-contract.md).

Design review acceptance does not mean runtime, visual, audio, timing or player-comprehension acceptance.
